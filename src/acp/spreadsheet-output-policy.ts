export const SPREADSHEET_OUTPUT_POLICY = [
  "面向用户的表格统一使用 Excel（.xlsx）。",
  "即使用户口头提到 CSV、纯数据导出或机器交换，也应交付 .xlsx；只有服务内部兼容读取历史 CSV 时才保留 CSV。",
  "更新历史 CSV 时，服务会在同一文件资产下创建新的 XLSX 版本，保留原 CSV 版本、文件夹位置和自动化绑定。",
  "新建或更新表格时不得提交 .csv 文件名或 text/csv 内容。",
  "默认单个 Excel 工作簿最多保留 100 条数据行（表头不计入）；超过 100 行时只写入前 100 行，并在回复中说明已按默认展示上限截断。",
  "这里的“前 100 行”严格按本轮已取得数据的原始顺序，不得宣称是全量排名或最优 100 条；用户明确要求全部、完整导出、不要截断或指定更大数量时，才可突破该默认上限。",
].join("");

export const OUTPUT_VOLUME_POLICY = [
  "数量较大的结果默认采用受控展示：量化选股默认最多展示工具返回顺序的前 100 个候选；超过 100 个时必须明确说明已截断，不能把这 100 个说成全市场排名。",
  "只有用户明确要求全部结果、完整名单、不要截断或指定更大数量时，才突破 100 个候选的默认展示上限；如果工具本身也有上限，必须同时说明工具上限。",
  SPREADSHEET_OUTPUT_POLICY,
].join("");
