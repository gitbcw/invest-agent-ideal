# 发布记录：20260911T161500Z-0824e856

按 [release-governance-evidence-template.md](./release-governance-evidence-template.md) 口径记录。

## 变更摘要

```text
release_id: 20260911T161500Z-0824e856
commit: 0824e856（main）
branch: main
date: 2026-09-11 15:55–16:05（北京时间，白天发布惯例）
owner: 用户授权（2026-09-11 对话：诊断复盘推送断供后指示「修复」）
agent 执行
change_type: code + 生产任务修订（服务层格式类不变量，无安全面改动）
```

- 背景：owner 反映「很久没收到复盘推送」。生产证据链：复盘 typed 任务自 8-17 迁移以来逐日 succeeded，但三个用户全部日/周/月复盘任务 `delivery_json={"mode":"none"}`，`deliverResult` 见 none 即记 not_requested，**复盘简报自迁移日起从未发起过一次推送**（push_jobs 中 `message_kind='automation_summary'` 无任何复盘任务来源；最后成功 daily_review 推送 mg/111=8-12、dyk=8-14）。迁移只给了盯盘 wechat_summary，复盘给了 none——与 `scheduled-message-policy.ts`（daily/weekly/monthly_review 均定义投递重试与过期策略）的设计契约矛盾。晚间窗口仅有的几条推送是 automation_failure/recovery 通知（9-8/9-9）。另发现：模型运行总结写「微信简报已推送」系照提示词承诺的虚假声明（工具白名单无推送工具，模型不可能推）。
- 代码修复（commit 0824e856）：
  1. `deliverResult`：复盘类任务（resolveGenericAutomationReviewTarget 命中）推送正文优先回读发布后落库的 pushBrief（daily=dailyPlanBackend、weekly/monthly=periodicReviewBackend 的 summary 字段）——模型最终回复是面向审计的元叙述（含 reviews.save/artifactId 字样），不再直接推送；读取失败回退质量下限（40 字）保护下的运行总结，不静默不推。
  2. 复盘任务不再被 shouldNotify 压制（wechat_summary 到点必推，与 BC-20260904-001 语义一致；守卫同时防住 wechat_on_condition 复盘任务被误压制）。
  3. 回归测试 ×2：pushBrief 优先（断言微信正文不泄漏工具名/artifactId）+ backend 不可读时回退。
- 生产数据动作（owner 已授权，先备份）：`data/runtime.db.bak-pre-reviewpush-20260911`（better-sqlite3 backup API）后，经 `scripts/fix-review-delivery.mjs` 走服务层 revision 链（updateAutomationTask+activateAutomationTask，edit_source=script）把 8 个复盘任务提交 rev2：delivery=`wechat_summary`，validityMinutes 日 780/周 2880/月 4320（对齐 scheduled-message-policy 过期语义：日推次日 ~08:20 前有效、周推次交易日 09:00、月推 +72h）。111 已归档月复盘跳过；全部任务保持 active，next_run 不变（111 日 19:00 / dyk 19:10 / mg 19:20，周六周复盘 09:00）。`.env`、Workspace、`reviews/`、`.state/`、微信状态零触碰。
- 调度器 `listDueAutomationTasks` 每 tick 现查 DB 并按 current_revision 装载，无内存缓存，修订即刻生效、无需再重启。

## 验证证据

| 门类 | 结果 | 证据 |
| --- | --- | --- |
| 确定性测试/类型检查 | pass | `npm test` 671/671（新增 2 例）；`tsc --noEmit` 零错 |
| pushBrief 优先 | pass | 回归测试断言 push_jobs.message=发布 summary、不含 reviews.save/artifactId |
| 回退路径 | pass | artifact 存在但 backend 无 summary 时推送运行总结（质量下限保护） |
| 生产部署 | pass | deploy-volcano.sh 全流程，PM2 双进程 online，runtime /health ok，release.json=20260911T161500Z-0824e856 |
| 任务修订 | pass | 8 任务 rev2 delivery/schedule/instruction 逐项核对；audit 行 task.revision_created→task.activated（nextRunAt 与原计划一致）；111 archived 跳过 |
| 备份 | pass | data/runtime.db.bak-pre-reviewpush-20260911（71MB，backup API 一致性快照） |

## 灰度与观测

- **首验：今晚 19:00/19:10/19:20（111/dyk/mg）日复盘自然 run**——预期 push_jobs 出现 origin_task_key=migrated_scheduled-daily-review_* 的 automation_summary、delivery_status=pending→sent、微信收到 pushBrief 正文。明晨（周六）09:00 周复盘为第二个验证点。
- 已知边界（设计内，非遗留）：微信 24h 互动窗口（owner 2026-09-11 口径）过期时推送挂起 awaiting_user，超 validityMinutes 后判死不补发（2026-09-03 事故口径）；日复盘窗口 780min≈次日 08:20。用户需与微信侧保持互动否则会偶发收不到。
- 遗留（owner 裁决项）：~~①111 盯盘任务 delivery 无 validityMinutes~~（**已解决**：owner 同日授权，rev6 补 validityMinutes=120，ref=fix-111-market-watch-validity-20260911，next_run 不变）；②mg 19:30 持仓复盘工作簿任务（at_60d62fcb）delivery 仍为 none，该任务推送本就不在本次范围（其产物是工作簿不是简报）；③模型总结中「微信简报已推送」字样在修复后语义已成立（服务层确会投递），提示词未改动（摩擦驱动哲学：无摩擦不动提示词）。
- 回滚：代码=`git revert 0824e856` 重发；任务修订=重跑 fix 脚本改回需先按备份 DB 恢复指针（revision append-only，rev1 原样保留）；备份文件在灾备排除清单外按 7 天保留策略处理。
