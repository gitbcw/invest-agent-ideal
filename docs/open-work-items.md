# 待办工作项清单（owner 口述，Agent 维护）

> 本文档是 owner 与 Agent 之间的工作项台账：owner 口述需求记入，Agent 逐项实现并验收后关闭。
> 新增项追加到文末；实现细节在设计确认后回填到对应小节。
> 建档：2026-08-17（上线日）。当前批次围绕「模型选择体验 + 用量可观测」。

## W1 · 自动模型路由（设计已确认 2026-08-17）

**状态**：**P1 已实现并部署（2026-08-17）**——first_token_ms 采集、model-health 服务（真实反馈判定 + 降级防抖 + 30 分钟冷却乐观回升）、自动链接线（chat 与自动化任务）、UI「自动」默认项。实测：新轮次 model_source=auto，首字延迟已入库（首个样本 62.5s>30s，构成 sol 首个坏证据，连续第二次慢将降级 terra）。P2 探针与缓刑回升、P3 轮内兜底重试已于 2026-08-17 部署（探针启动即捕获 sol 20s 超时并计入证据；45s 首字看门狗 + 换模型重试一次 + 失败证据入健康）。

### 用户语义

- 模型下拉默认第一项「自动（推荐）」，副标签实时显示当前路由结果（如「自动 · GPT-5.6 Sol」）。
- 手动选择具体模型 = 锁定，不参与路由；不发 model 字段的调用（微信端、自动化任务）一律走自动。

### 候选链（能力过滤 + 质量优先级）

| 顺位 | 模型 | 进入条件 |
| --- | --- | --- |
| 1 | gpt-5.6-sol | 健康 |
| 2 | gpt-5.6-terra | 顺位 1 不健康 |
| 3 | gpt-5.5 | 顺位 1-2 不健康 |
| 4a | doubao-seed-2-1-turbo | 兜底，且本轮含图片 |
| 4b | deepseek-v4-pro | 兜底，无图片（flash 仅手动可选，2026-08-17 owner 修订） |

- luna、deepseek-v4-pro 不进自动链（手动可选）。
- 图片判断依据本轮消息附件类型（豆包接图片、DeepSeek 接纯文本）。

### 健康状态（双信号源 + 防抖）

- **信号 A 真实调用反馈（主力）**：每轮结束从 trace 记录该模型首字延迟与成败；需新增 `agent_traces.first_token_ms`（流式首 chunk 时间）。
- **信号 B 小时探针（兜底）**：每小时对链上 5 模型各发一次 `max_tokens=1` 补全测首字延迟（约 120 次/天，成本忽略不计），为不常用模型提供回升依据。
- **判定**：健康 = 近 30 分钟真实调用 p50 首字 < 30s 且错误率 < 20%；无真实调用时以最近探针（< 30s）为准。
- **降级防抖**：连续 2 个证据（两次慢调用，或 1 次失败 + 1 次探针慢）才降级。
- **回升防抖**：连续 2 个探针周期健康才升回，避免边界横跳。
- 全链不健康：按优先级硬选第 1 位。

### 执行语义（两层兜底）

1. 选时路由：每轮开始按当时健康状态选模型。
2. 轮内兜底（P3）：选中模型 45s 无首字或直接报错 → 本轮立刻换下一顺位重试一次；失败计入健康状态。

### 实施切分

- **P1**：first_token_ms 采集 + 健康状态服务（真实反馈判定 + 降级防抖 + 30 分钟冷却回升）+ 自动链路由 + UI「自动」默认项。
- **P2**：小时探针 + 探针驱动回升。
- **P3**：轮内 45s 兜底重试（动流处理，单独充分测试）。

### 可观测性

- trace 记 `agent_model` + `model_source=auto|manual`（字段已有）；健康状态定期快照落库，平台管理端可查。

## W2 · 模型选择器信息升级（两行展示 + 价格）

**状态**：**已实现并部署（2026-08-17）**——ModelPicker 两行展示 + 峰/闲价格标签 + 自动默认项；models.state 命令 + /api/models。gpt-5.5 价格行已补。

- 下拉展开后每个选项两行：
  1. 模型名称（主行）；
  2. 灰色小字：一句话说明模型定位（下方文案草案）。
- 价格标签（2026-08-17 owner 修订）：显示**输入/输出双价**，格式 `¥2/12`（每百万 tokens，峰谷按峰值），峰谷模型统一按**峰值**显示；下拉底部注明「价格为每百万 tokens 输入价（峰谷模型按峰值）」。实际计费仍按注册表峰谷规则计算，不受展示简化影响。
- luna 已禁用，不出现在选择器。
- **附带修补**：`gpt-5.5` 当前不在 `model-pricing.ts` 注册表中（UI 可选但计价走兜底标记），需补价格行。

### 文案与价格草案

> 2026-08-17 owner 定价口径：GPT 系列按美元牌价走中转折扣——美元数值 1:1 视为人民币基数再乘系数（默认 0.4，区间 0.2~0.4，`GATEWAY_USD_RELAY_RATE` 可覆盖）；terra 牌价修正为 $2.5/$15；gpt-5.5 与 sol 同价 $5/$30（注册行已补）。

| 模型 | 第二行文案 | 价格（元/百万 tokens，输入/输出） |
| --- | --- | --- |
| 自动（推荐） | 按质量优先自动选择，异常时逐级降级 | 显示当前路由结果 |
| gpt-5.6-sol | 旗舰质量，复杂分析与长推理首选 | ¥2/12 |
| gpt-5.6-terra | 高质量均衡档，日常深度分析推荐 | ¥1/6 |
| gpt-5.5 | 上代旗舰，质量稳定，速度通常更快 | ¥2/12 |
| deepseek-v4-pro | 深度思考档，中文与工具调用强 | ¥9/27（峰值） |
| deepseek-v4-flash | 极速性价比档，仅手动可选 | ¥3/9（峰值） |
| 豆包 Seed 2.1 Turbo | 多模态档，支持图片理解 | ¥6/30 |

## W3 · 用户菜单 + 使用记录 / 使用统计页

**状态**：**已实现并部署（2026-08-17）**——左下角用户菜单「使用记录」入口 + /usage 页（时间筛选/汇总卡片/按天与按模型图表/分页明细），usage.summary 与 usage.records 走 connector。

- 入口：门户左下角用户头像位菜单，点击展开含「使用记录」。
- 「使用记录」页（标准统计页设计）：
  - 时间查询：今天 / 近 7 天 / 近 30 天 / 自定义区间；
  - 汇总卡片：总开销（元）、调用次数、总 tokens；
  - 图表：按模型开销分布、按天开销趋势、（可选）时延分布；
  - 调用记录明细表：时间、模型、来源（自动/手动）、会话入口、输入/输出 tokens、费用、耗时。
- 数据源：`agent_traces`（个人维度查本人；已有 cost_amount/cost_currency/tokens/agent_model/elapsed_ms）。
- 平台侧管理视图（admin 成本总览）已有设计见 [cost-statistics-design.md](./cost-statistics-design.md)，本页为终端用户个人视角，不重复建设管理端能力。


## W4 · 行情数据源韧性治理（回放实测暴露，2026-08-17）

**状态**：**连接韧性已实现并部署（2026-08-17）**——进程级共享连接（消灭每轮重连）+ 3 次指数退避重连 + 最终失败降级为无工具继续作答（不再判死轮次）+ 10 分钟蓝绿换新；实测验证轮行情数据正常。重查询批量化/缓存已由数据工具侧上线（2026-08-17 傍晚），调用方补设 MDT_REALTIME_TTL=60。复验（收盘后时段）：原失败用例连续两轮成功（回放期 4/38 直接失败→0 失败），Agent 侧 9 次工具调用走批量接口、无逐行业循环，数据诚实性提升；但时延 235s→125s（缓存生效中）仍远于 60s 验收线，瓶颈在数据工具上游取数（其自述资金流无降级链、akshare 走东财 push2 被 IP 限流）——待数据工具侧继续优化，并需次日盘中复验（原始故障场景为盘中）。依据：mg 全量回放 164 轮中 16 轮无回复，全部集中在盘中实时拉全市场数据的重负载场景（行业资金流复盘 4、公式验证长会话 10、周复盘 1、指标分析 1）；日志直接证据为 `market-data-tool` MCP 连接超时（3 秒握手失败即终）与 LLM 上游偶发 `upstream failed`。

- 行情 MCP 连接/调用失败增加重试与退避（当前一次超时即整轮失败）。
- 重负载问题的市场数据查询做批量化/缓存（行业资金流单轮要打满全市场）。
- 与 W1-P3（轮内换模型重试）互补：一个救数据源，一个救模型上游。

## W5 · 部署重启优雅排空

**状态**：已实现并部署（2026-08-17，commit 792928d）。shutdown 等待在途轮次最长 240s。2026-08-18 复盘补充：排空要生效还依赖 PM2 条目携带 `kill_timeout`（300s，ecosystem.mastra.config.cjs）；8-17 晚 20:45 的中断发生在旧进程条目（默认 kill_timeout ~1.6s）上，SIGKILL 早于排空。8-18 14:36 重建的条目已带 kill_timeout=300000（`pm2 jlist` 可核对）。发布必须走 `pm2 restart`（保留条目），不要 `pm2 delete + start dist/index.js` 之外的旁路杀进程。

## 附 · 经典回归用例集（工具轻量化）

2026-08-17 全量回放（12 会话 164 轮，报告 `data/migration-golive-20260817` 同级 `data/replay-mgreplay-20260817/report.html`）只作为一次性摸底，**不再全量重跑**。日常回归用 9 条经典用例（约 10 分钟）：

```bash
# 以 mgreplay 账号重放经典用例（计划/输出均可覆盖）
cd data/replay-mgreplay-20260817
REPLAY_PLAN=./classic-plan.json REPLAY_RESULTS=./classic-results.jsonl REPLAY_CONVMAP=./classic-conv-map.json node driver.mjs
```

用例覆盖：周复盘（重）、行业资金流（最重）、个股+自选写操作、策略问答、公式分析×2、个股趋势（轻）、公众号链接（外部依赖）、附件缺失边界。mgreplay 账号与 connector 已常驻，状态导入（持仓/记忆/偏好）随当日迁移完成。

## W6 · 微信在途轮次的中断源排查

**状态**：已修复（2026-08-18）。8-17 现象的两个根因：
- 根因一（主因）：当时 PM2 进程条目仍是默认 kill_timeout（~1.6s），SIGINT 后主进程被 SIGKILL，240s 排空从未真正执行（日志实锤：SIGINT 后 0.9s 新进程即启动）。已随 W5 的 ecosystem kill_timeout=300000 生效修复。
- 根因二：微信回复由 fork 出的 listener worker 内 SDK 直接送达；worker 收到 SIGINT/stop 原本立即 abort 退出，即使主进程排空跑完在途轮，回复也无处转发。修复：worker 停止接收新消息后等待在途 chat 完成并送达再退出（宽限期 `INVEST_AGENT_WEIXIN_SHUTDOWN_GRACE_MS`，默认与排空预算对齐 240s；父进程断开则立即退出）。停止窗口内的新微信消息回复明确的发布提示而不是静默丢失。
- 验收方式：下一次发布窗口若有微信在途轮，观察「优雅排空中」日志后主进程存活至轮次完成、worker 在回复送达后退出。

## 附 · 微信通道修复记录（2026-08-17，已完成）

- 根因一：发版重启杀监听且不自愈 → `PLATFORM_WEIXIN_AUTO_START=true`（owner 授权），实测重启后自动恢复 invest-agent-111 监听。
- 根因二：生产迁移的微信会话行 `project_id` 为旧口径 `invest-agent-<user>`，与新系统 `invest-agent` 白名单不匹配 → `CONVERSATION_SCOPE_MISMATCH` 拒收；已规范化 16 会话/658 消息。
- 验证：owner 实机「你好」20 秒收到回复，「日复盘」会话续聊正常。

## 附 · 用户文件迁移缺口补齐（2026-08-17，已完成）

- 现象：111 的「我的文件」多数预览报「文件内容不可用」。审计：66 个在册资产仅 29 个文件在位。
- 根因：cutover 整库拷贝带过来了 user_assets/user_asset_versions 记录，但这些记录指向的存储文件在生产的**旧工作区布局**（`invest-agent-data/workspaces/<user>/assets/<id>/`），不在六域迁移范围，未随 data/projects 重建。
- 修复：从生产按 asset id 补拷 37 个目录到新 digest 根（只读拷贝，生产未动）；复核 66/66 在位。
- 教训（后续 runbook 修订要点）：**任何数据刷新流程必须包含「user_assets 文件从旧工作区布局到 digest 根的搬运」步骤**；生产退役后文件可从 DR 备份的 workspaces 快照恢复。

## 附 · 上线 24h 诊断修复记录（2026-08-18）

- **C2 · 旧会话 403（已修复）**：mg 等 3 个真实用户在迁移前 web 会话发送消息全部 `CONVERSATION_SCOPE_MISMATCH`（HTTP 403），根因与 8-17 微信通道根因二相同（旧口径 `project_id='invest-agent-<user>'`），8-17 的规范化只覆盖了微信通道。8-18 晚以 `scripts/normalize-legacy-conversation-project.mjs`（dry-run→备份→apply）规范化全部 62 会话/697 消息；备份 `data/runtime.db.pre-c2-normalize-2026-08-18T1131`。
- **C1 · 单会话 7 万条 NULL message_id 复制风暴（已闭环）**：portal 镜像 `upsertMessage` 的 `ON CONFLICT(message_id)` 无法去重 NULL 行，会话同步每轮把 NULL 行原样再插（每 pass 上限 ~10k）。防御：portal `upsertMessage` 拒绝空 messageId、detail sync 跳过无 id 消息。数据清理已获 owner 授权并于 2026-08-18 晚执行：`scripts/dedupe-null-message-id-rows.mjs` 删除 70,110 条重复、按组保留 8 条真实轮次唯一副本（洪水轮次无 canonical 行，保留副本是唯一记录），表 71,988→1,882 行；备份 `data/runtime.db.pre-c1-dedupe-2026-08-18T1204`。
- **C4 · 模型兜底加强**：自动路由轮内兜底从一跳扩展为走完整条自动链（`MASTRA_AUTO_FALLBACK_MAX`，默认 3 跳），网关 upstream 错误（如 `Upstream error: 400`）纳入可重试签名（大小写不敏感）。8-18 上午 111 的 market-watch 失败即「deepseek 首字超时→兜底到 sol→sol 网关 400→无下一跳」链路。
- **观测缺口**：`external_mcp_tool_calls` 随 ACP 退役停写；已在 W4 共享连接收口点（`withExternalToolCallObserver`）恢复记录（chat 与 scheduled 两条路径）。`conversation_task_runs` 为 ACP 时代死台账（无写入方，仅启动恢复引用），在 table-ownership 标注 legacy。

## W7+ · owner 待补充

（owner 口述新工作项追加于此，Agent 负责整理成条目并回填细节。）
