# 发布记录：20260906T041635Z-6e571551

按 [release-governance-evidence-template.md](./release-governance-evidence-template.md) 口径记录。含 [SCR-20260906-04](./semantic-change-regression-gate.md)（tier-2：数据方法与自动化产出契约，commit message 落档）。发布后随即执行了 mg 两任务的生产 schema 对齐（§发布后动作）。

## 变更摘要

```text
release_id: 20260906T041635Z-6e571551
commit: 6e571551b8ed9cdc62834cea1b9736f6a52dbcb0（main，committed-local-main）
branch: main
date: 2026-09-06 12:16–12:18（北京时间，白天发布惯例）
owner: 用户授权（2026-09-06 对话：「完成479之后做480」，任务契约含发布与对齐）
agent 执行
change_type: code（T-480 工作簿 schema 契约 + mg 对齐）
```

- 变更目的：T-480——任务 revision 与工作簿 schema 建立持久契约（expectedSchema），schema 漂移在模型执行前零成本阻断，monthly rollover 新文件补上结构校验；按生产实测把 mg 行业复盘（17 列）/持仓复盘（10 列）两个任务对齐到正式 schema（证实 owner 2026-08-31 T-417 裁决，"14 列"为过时巡查口径）。契约见 [automation-workbook-schema-contract.md](./automation-workbook-schema-contract.md)。
- 影响范围：`src/services/automation-spreadsheet.ts`（schema 快照/比对）、`src/services/automation-tasks.ts`（契约字段+自动快照）、`src/services/generic-automation-runner.ts`（run 前 fail-fast + rollover create 校验）、测试 ×2、对齐脚本、契约文档与 README 索引。
- 明确不影响的生产状态：`.env`、Workspace、`reviews/`、`.state/`、微信状态零触碰；历史资产版本原样保留；生产 DB 仅通过 `updateAutomationTask`（append-only revision + 审计）新增两条 revision，无行级直写。
- 依赖与外部条件：无新增外部依赖。

## 验证证据

| 门类 | 结果 | 证据 |
| --- | --- | --- |
| 确定性测试/类型检查/构建 | pass | `npm run verify`：659 测试 0 fail（首跑 1 例 flaky，复跑两次全绿）、agent-context 184 文档、build、boundary 7/7 |
| scope/权限/确认/revision/幂等 | pass | 契约走 revision append-only；对齐脚本幂等（复跑 "OK already aligned"） |
| 错误终态、超时、取消、重试 | pass | schema mismatch → invalid_input 不可重试，分类正确（INVALID 语义） |
| 语义门 SCR-20260906-04 | pass | tier-2；四栏：程序 pass / 语义 pass（5 用例确定性断言：兼容快照、漂移阻断 executor 零调用、升级重快照、存量不校验、rollover 拒绝）/ 性能 n.a. / 业务终态待对齐后自然 run；go-with-isolation |
| 隔离行为评估 | pass | 无契约存量任务行为回归用例在案（appendRows 守卫不变） |
| 故障演练 | n/a | 未改故障面；回滚=纯代码 revert + 对齐 revision 可由后续编辑自然覆盖 |

## 发布执行

- 快照：verified 20260906T041635Z-6e571551（发布前 git stash -u 临时收起其他任务两份待验收文档，部署后恢复）。
- 部署：`release-deploy deploy`——rsync → Portal 重建 → 双进程重启；尾部一次 23655 Connection refused 为重启时差（历次发布同现象）。
- 发布窗口核对：周六 12:16（无盘中调度命中）。

## 发布后最小验收

1. `/health` 200、Portal `/api/health` 200 ✓；PM2 双进程 online ✓
2. `smoke:mcp-service-tools` 48 工具 ok ✓
3. dist 符号核验：`snapshotWorkbookSchema` 在生产 automation-spreadsheet.js ✓
4. app.log 近 60 行 ERROR 0 ✓
5. known-good 标记完成，裁剪旧档 20260903T001938Z-609c9884

## 发布后动作：mg schema 对齐（T-480 核心交付）

- `scripts/align-mg-workbook-schema.mjs`（幂等）dry-run 后实跑：
  - 行业复盘 `at_60d62fcb…`：rev20（无契约）→ **rev21 expectedSchema 17 列 headerRow=1**，审计 `edit_source_ref=t480-workbook-schema-align`，重新激活，next_run 2026-09-07T11:30Z（与对齐前一致）
  - 持仓复盘 `at_d64649ad…`：rev7 → **rev8 expectedSchema 10 列**，同上，next_run 2026-09-07T09:30Z（一致）
- 复跑幂等验证：两任务 "OK already aligned" ✓

## 灰度与观测

- 观察窗口：发布后 7 天（至 2026-09-13）——
  - 9-07 起 mg 两任务自然 scheduled run 全绿（17/10 列契约兼容）；
  - `AUTOMATION_SCHEMA_MISMATCH` 零误触发（出现即视为契约误判，需复核绑定资产）；
  - 10-01 月度 rollover：新 10 月文件按 17 列契约校验（P-001 场景的结构防线首验）。
- 回滚触发：mg 自然 run 因 schema 误拒、或 appendRows 守卫行为回归；回滚=`git revert 6e57155` 后重新发布（对齐 revision 不需回滚——旧代码忽略 expectedSchema 字段）。

## Go / No-Go

```text
G1 反馈证据：go（8-28 三连败证据 → 契约缺口 → 生产实测 → 对齐落地）
G2 服务边界：go（boundary 7/7；对齐走服务 API 非直写）
G3 失败终态：go（mismatch=invalid_input 不可重试；不改变既有终态机）
G4 灰度与回退：go（快照/known-good；对齐 revision 对旧代码无害）
G5 关联与脱敏：go（审计 edit_source_ref 可追溯；契约只存表头标签不涉数据行）

最终结论：go（生产已部署、验收、对齐完成）
未解决风险：①headerRow=1 口径来自合并标题行 17 格非空的既有判定，契约与运行时守卫同源故自洽，但与巡查报告"表头第 2 行"的人工口径不同——已记录在契约文档；②9-07 首次自然 run 与 10-01 首次 rollover 为两个待验证点
批准人：用户（2026-09-06 对话授权）
复核日期：2026-09-06；下次复核：2026-09-07 自然 run 后 + 2026-09-13 观察窗口收口
```
