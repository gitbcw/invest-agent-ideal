"use client";

import { AutomationWorkspace } from "./AutomationWorkspace";

/** Compatibility entry point used by the original /automations page. */
export function AutomationShell() {
  return <AutomationWorkspace />;
}
