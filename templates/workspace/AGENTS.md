# Legacy Workspace Compatibility Marker

legacy_workspace_compatibility_only: true

This directory is retained only for read-only migration and compatibility tooling. The Mastra Agent does not load this file or any Skill below this legacy Workspace. Current Agent instructions live in service code; user methodology lives in the registered Mastra project's `skills/` and `methods/` directories.
